<resources>

    <!-- Base application theme. -->
    <style name="MaterialTheme"  parent="ParentMaterialTheme">

    </style>

    <style name="ParentMaterialTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="colorButtonNormal">@color/orange_400</item>
        <item name="colorControlHighlight">@color/orange_100</item>
        <item name="colorControlActivated">@color/colorAccent</item>
    </style>

    <style name="ToolbarTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColorPrimary">@color/grey_100</item>
        <item name="android:textColorSecondary">@color/grey_100</item>
    </style>

    <style name="PopupTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColorPrimary">#424242</item>
        <item name="android:background">#eeeeee</item>
    </style>

</resources>
